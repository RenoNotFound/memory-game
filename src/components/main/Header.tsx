import React from "react";

const Header: React.FC = (): JSX.Element => {
  return (
    <header>
      <img src="logo192.png" alt="cartoon cat" width="100px" />
      CatPairs
    </header>
  );
};

export default Header;
